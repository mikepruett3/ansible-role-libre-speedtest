---
# defaults file for ansible-role-libre-speedtest

base_url: "https://api.github.com/repos/librespeed/speedtest-cli/releases/tag"
current_version: "v1.0.12"
url: "{{ base_url }}/{{ current_version }}"
